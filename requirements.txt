# الأساسيات والتنفيذ
python-telegram-bot==20.6  # تحديث للإصدار الأحدث مع إصلاح الأخطاء
fastapi==0.109.1  # إصدار أحدث مع تحسينات أداء
uvicorn==0.27.0  # دعم أفضل للإصدارات الحديثة
python-multipart==0.0.9  # إصلاح ثغرات أمان

# معالجة الوسائط
yt-dlp==2024.4.9  # أحدث إصدار مع دعم للمواقع الجديدة
aiofiles==23.2.1  # تحسينات الأداء

# قواعد البيانات
sqlalchemy==2.0.25  # إصلاح مشاكل التوافق
alembic==1.13.1  # لإدارة تحديثات قاعدة البيانات (مigrations)

# البيئة والتحقق
python-dotenv==1.0.1  # إصلاح ثغرات أمان
pydantic==2.6.4  # إصدار رئيسي مع تحسينات كبيرة

# التطوير والاختبار
faker==24.9.0  # إضافة ميزات جديدة للبيانات الوهمية

# مكتبات إضافية موصى بها (اختيارية)
loguru==0.7.2  # تحسين نظام السجلات
httpx==0.26.0  # للطلبات HTTP المتزامنة
python-dateutil==2.9.0  # معالجة التواريخ